// Populate only with verified IDs after the site's consent interface is ready.
// A missing ID keeps that provider disabled.
window.WROC_ANALYTICS_CONFIG = Object.freeze({
  ga4MeasurementId: null,
  metaPixelId: null,
});
