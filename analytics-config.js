// Provider tags load only after the visitor grants the matching category.
// A missing ID keeps that provider disabled.
window.WROC_ANALYTICS_CONFIG = Object.freeze({
  ga4MeasurementId: "G-6FWP69HHWG",
  metaPixelId: null,
});
